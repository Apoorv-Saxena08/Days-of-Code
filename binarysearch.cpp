#include<iostream>
using namespace std;

int main(){
    //binary search code
    arr[3] = {1,2,3};
    int n = arr.size();
    int start = 0 , end = n-1, mid ;
    while (start<=end){
     mid = start + (end-start)/2;
     if(f)
    }
}
